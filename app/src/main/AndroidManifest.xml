<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.project3">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:windowSoftInputMode="stateVisible|adjustPan"
            android:screenOrientation="portrait">
            <!-- Para obligar a la aplicacion a que se use en modo retrato uso screenOrientation = portrait -->
            <!-- Para obligar a la actividad a desplazarse cada vez que una vista vaya a recibir el foco pero esta quede oculta por el teclado,
             y que el teclado quede siempre visible, uso windowSoftInputMode = stateVisible|adjustPan-->

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>